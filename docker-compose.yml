services:
  fitcoach-ia:
    container_name: fitcoach-ia
    restart: unless-stopped
    image: fitcoachia/fitcoach-app:${VERSION:-latest}
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
