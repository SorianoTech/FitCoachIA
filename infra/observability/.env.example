GF_ADMIN_PASSWORD=changeme
DEPLOYMENT_ENVIRONMENT=dev
# Debe coincidir con las credenciales del Postgres de la app (docker-compose.yml)
FITCOACH_POSTGRES_DB=fitcoach
FITCOACH_POSTGRES_USER=fitcoach
FITCOACH_POSTGRES_PASSWORD=changeme
