GF_ADMIN_PASSWORD=changeme
DEPLOYMENT_ENVIRONMENT=dev
# Deben coincidir con las credenciales de cada Postgres (docker-compose.dev.yml / docker-compose.yml)
FITCOACH_POSTGRES_DEV_DB=fitcoach
FITCOACH_POSTGRES_DEV_USER=fitcoach
FITCOACH_POSTGRES_DEV_PASSWORD=changeme
# Opcionales hasta que despliegues produccion; el datasource "PostgreSQL Prod" fallara sin ellas.
FITCOACH_POSTGRES_PROD_DB=fitcoach
FITCOACH_POSTGRES_PROD_USER=fitcoach
FITCOACH_POSTGRES_PROD_PASSWORD=changeme
